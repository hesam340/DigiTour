import { ToastContainer } from "react-toastify";

const ReactToastify = () => {
  return (
    <ToastContainer
      position="top-center"
      pauseOnFocusLoss={false}
      pauseOnHover={false}
    />
  );
};

export default ReactToastify;
